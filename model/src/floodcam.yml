# conda env create --file floodcam.yml
name: floodcam
channels:
  - defaults
  - conda-forge
dependencies:
 - python
 - numpy
 - scikit-learn
 - scikit-image
 - pip
 - ipython
 - tqdm
 - pandas
 - seaborn
 - matplotlib
 - pip:
    - tensorflow-gpu==2.5
